services:
  - type: web
    name: custom-track-racer
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
    autoDeploy: true
